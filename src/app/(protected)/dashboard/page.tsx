import { DashboardContent } from "@/components/section-layouts/dashboard-content";

export const metadata = {
    title: "Dashboard",
};

export default function DashboardPage() {
    return <DashboardContent />;
}
